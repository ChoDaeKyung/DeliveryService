spring:
  application:
    name: select-front
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

server:
  port: 7007

swfm:
  service-url: ${SELECT_SERVICE_URL:http://localhost:9000}
emails-url: ${DOMAIN_URL:http://localhost:9000}
user:
  api-url: http://localhost:9010  # 백엔드 API URL 설정
kakao:
  api-key: 118f545aa38dcd31bad414ade22b9cd6
